document.querySelectorAll('.delete-form').forEach(f=>f.addEventListener('submit',e=>{if(!confirm('Tem certeza que deseja excluir este registro?')) e.preventDefault();}));
document.querySelectorAll('.js-validate').forEach(form=>form.addEventListener('submit',e=>{let errors=[];form.querySelectorAll('[required]').forEach(el=>{if(!el.value.trim()) errors.push(`Campo ${el.name} obrigatório`)});const email=form.querySelector('input[type="email"]');if(email&&email.value&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.value)) errors.push('E-mail inválido');const phone=form.querySelector('input[name="phone"]');if(phone&&phone.value&&!/^[0-9()+\-\s]{8,20}$/.test(phone.value)) errors.push('Telefone inválido');if(errors.length){e.preventDefault();alert(errors.join('\n'));}}));
