<form method='POST' class='glass card js-validate' action='{{ $clinicalNote ? route('clinical-notes.update',$clinicalNote) : route('clinical-notes.store') }}'>@csrf @if($clinicalNote) @method('PUT') @endif
<select name='patient_id' required>@foreach($patients as $patient)<option value='{{ $patient->id }}' @selected(old('patient_id',$clinicalNote->patient_id ?? '')==$patient->id)>{{ $patient->full_name }}</option>@endforeach</select>
<input name='title' value='{{ old('title',$clinicalNote->title ?? '') }}' required><textarea name='content' required>{{ old('content',$clinicalNote->content ?? '') }}</textarea><input type='date' name='created_on' value='{{ old('created_on',isset($clinicalNote)?optional($clinicalNote->created_on)->format('Y-m-d'):'') }}' required><button class='btn'>Salvar</button></form>
